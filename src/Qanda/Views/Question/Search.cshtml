@model List<Question?>

@{
  ViewData["Tile"] = "";
}

@if (Model!.Count > 0)
{
  <div class="questions">
    @foreach (var question in Model)
    {
      <partial name="_QuestionCardPartial" model="@question" />
    }
  </div>
}
else
{
  <h1 class="title">Not many great matches came back for your search</h1>
}